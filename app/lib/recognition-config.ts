export const CORRECT_CONFIDENCE_THRESHOLD = 0.75; // sits in your 75-85% range
export const INCORRECT_CONFIDENCE_THRESHOLD = 0.45; // matches Python's floor — anything reported at all is at least this confident
